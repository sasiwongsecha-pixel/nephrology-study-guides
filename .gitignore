# ── Backups & autosaves ───────────────────────────────
*.bak
*.bak.*
*~
*.swp
*.swo

# ── Local tooling / skill configs ─────────────────────
.claude/
.vscode/
.idea/

# ── Scratch directories (verification, raw assets) ────
screenshots/
uploads/
agent_outputs/

# ── OS junk ───────────────────────────────────────────
Thumbs.db
desktop.ini
.DS_Store

# ── Session handoff (local working note, not for the repo) ──
HANDOFF.md

# ── Local data exports (not part of the published guides) ──
cases.json
cases.txt
flashcards.json
mindmap.json
quiz.json
study_guide.md

# ── Sibling / scratch HTML guides not part of the published deliverable ──
Sodium Handling Deep Dive.html
nephrology_v2.html
brenner_nephrology.html
diag-preview.html

# ── Copyrighted Brenner figure crops — NEVER commit (personal study only) ──
# Real image bytes live only on the local machine + the private Apps Script build.
figures/brenner/

# ── Private Apps Script build: generated artifact with inlined copyrighted figures ──
# (appsscript/Code.gs + appsscript/appsscript.json ARE committed — they hold no figures)
appsscript/index.html
# ── clasp credentials / project id (user-specific, keep private) ──
.clasp.json
.clasprc.json
